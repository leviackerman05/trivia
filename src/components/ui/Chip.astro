---
/**
 * Chip ([AIRBNB] d5/d8): pill-shaped labels. filter: Rausch-tinted pill
 * (the category-strip pattern). status: severity-tinted pill.
 */
interface Props {
  variant?: 'filter' | 'status';
  status?: 'success' | 'warning' | 'error';
  class?: string;
}

const { variant = 'filter', status = 'success', class: className } = Astro.props;

const statusClasses: Record<NonNullable<Props['status']>, string> = {
  success: 'bg-success-soft text-success-strong',
  warning: 'bg-warning-soft text-warning-strong',
  error: 'bg-danger-soft text-danger-strong',
};
---

<span
  class={`inline-flex items-center gap-1.5 rounded-pill px-3.5 py-1.5 text-xs font-semibold ${
    variant === 'filter'
      ? 'border border-primary/40 bg-primary/15 text-primary-strong'
      : statusClasses[status]
  } ${className ?? ''}`}
>
  <slot />
</span>
