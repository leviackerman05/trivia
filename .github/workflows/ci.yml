name: CI

on:
  pull_request:
  push:
    branches: [main]

jobs:
  ci:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: partybrain
          POSTGRES_PASSWORD: partybrain
          POSTGRES_DB: partybrain
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U partybrain"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      DATABASE_URL: postgresql://partybrain:partybrain@localhost:5432/partybrain
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 11

      - uses: actions/setup-node@v4
        with:
          node-version: 26
          cache: pnpm

      # Workspace install: root Astro app + @partybrain/server.
      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Format check
        run: pnpm format:check

      - name: Lint
        run: pnpm lint

      - name: Typecheck (astro check)
        run: pnpm typecheck

      - name: Unit tests
        run: pnpm test:unit

      - name: Apply database migrations
        run: pnpm --filter @partybrain/server db:deploy

      - name: Server tests (integration)
        run: pnpm --filter @partybrain/server test

      - name: Server build (tsc)
        run: pnpm --filter @partybrain/server build

      - name: Build (astro build)
        run: pnpm build

      - name: Smoke test (serves dist/ and verifies key routes)
        run: pnpm smoke
